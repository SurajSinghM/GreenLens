## Supabase integration removed from this project.
## Keep server-only secrets in your host (Vercel) or in a local .env (git-ignored).
# Server-only (DO NOT put a real key here)
# LOVABLE_API_KEY=your_lovable_or_gemini_api_key_here
